# Feightly.ai Backend Environment Variables
# Copy this file to .env and fill in the values

# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=your-account-id-here

# DynamoDB Table Names (auto-populated after deployment)
LOADS_TABLE_NAME=Feightly-Loads
DRIVERS_TABLE_NAME=Feightly-Drivers
NEGOTIATIONS_TABLE_NAME=Feightly-Negotiations
DOCUMENTS_TABLE_NAME=Feightly-Documents
BOOKINGS_TABLE_NAME=Feightly-Bookings

# S3 Bucket Name (auto-populated after deployment)
DOCUMENTS_BUCKET_NAME=feightly-documents-{account-id}

# Amazon Bedrock Configuration
BEDROCK_MODEL_ID=anthropic.claude-3-haiku-20240307-v1:0

# n8n Webhook Configuration
# IMPORTANT: Set this before deployment if using negotiation feature
# This URL will receive negotiation emails for delivery to brokers
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/feightly-negotiation

# API Configuration (auto-populated after deployment)
API_GATEWAY_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/prod

# Negotiation Configuration (optional - defaults used if not set)
DEFAULT_MAX_ROUNDS=4
DEFAULT_STRATEGY=moderate

# CORS Configuration (optional - defaults to * for development)
ALLOWED_ORIGINS=*

# Logging (optional - defaults to info)
LOG_LEVEL=info

# Notes:
# - Most values are automatically configured during CDK deployment
# - Only N8N_WEBHOOK_URL needs to be set before deployment if using negotiation
# - You can update Lambda environment variables in AWS Console after deployment
# - For production, restrict ALLOWED_ORIGINS to your mobile app domain
